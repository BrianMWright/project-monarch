# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# ── CI / Build artifacts ───────────────────────────────────────────────────
# Never commit build output or signing material.

# Android keystore — written at CI build time from ANDROID_KEYSTORE_BASE64 secret.
release.keystore

# Build output directories
build/

# Xcode export options plist — generated inline by the CI workflow.
ExportOptions.plist
release.keystore
